{
  "name": "paystream-protocol",
  "version": "0.1.0",
  "description": "PayStream Protocol — Privacy-preserving onchain payroll powered by Chainlink",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "fmt": "forge fmt",
    "snapshot": "forge snapshot"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.3.0",
    "@chainlink/contracts-ccip": "^1.6.4",
    "@chainlink/local": "^0.2.5"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "latest"
  }
}
